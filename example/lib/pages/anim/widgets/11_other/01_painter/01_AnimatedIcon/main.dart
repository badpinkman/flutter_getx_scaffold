import 'package:flutter/material.dart';

import 'anim_demo.dart';

class Main extends StatelessWidget {
  const Main({super.key});

  @override
  Widget build(BuildContext context) {
    return const AnimDemo();
  }
}
